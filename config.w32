// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("lime", "for lime support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("lime", "enable lime support", "no");

if (PHP_LIME != "no") {
	EXTENSION("lime", "lime.c");
}

